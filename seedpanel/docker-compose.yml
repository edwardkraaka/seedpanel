version: '3.8'

services:
  seedpanel:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: seedpanel-app
    ports:
      - "647:3000"
    environment:
      - NODE_ENV=production
    env_file:
      - .env
    restart: unless-stopped
    networks:
      - seedpanel-network

networks:
  seedpanel-network:
    driver: bridge
