services:
  seedpanel:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_SEED_PHRASE: ${NEXT_PUBLIC_SEED_PHRASE}
        NEXT_PUBLIC_CASE_NUMBER: ${NEXT_PUBLIC_CASE_NUMBER}
    container_name: seedpanel-app
    ports:
      - "647:3000"
    environment:
      - NODE_ENV=production
    env_file:
      - .env
    restart: unless-stopped
    networks:
      - seedpanel-network

networks:
  seedpanel-network:
    driver: bridge
